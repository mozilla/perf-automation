/// <reference types="react" />
import { IProtocol } from "matrix-js-sdk/src/client";
export declare const ALL_ROOMS = "ALL_ROOMS";
export declare type Protocols = Record<string, IProtocol>;
interface IProps {
    protocols: Protocols;
    selectedServerName: string;
    selectedInstanceId: string;
    onOptionChange(server: string, instanceId?: string): void;
}
declare const NetworkDropdown: ({ onOptionChange, protocols, selectedServerName, selectedInstanceId }: IProps) => JSX.Element;
export default NetworkDropdown;
