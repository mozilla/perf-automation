import React from 'react';
import { MatrixEvent } from "matrix-js-sdk/src/models/event";
import CallEventGrouper, { CustomCallState } from '../../structures/CallEventGrouper';
import { CallState } from 'matrix-js-sdk/src/webrtc/call';
interface IProps {
    mxEvent: MatrixEvent;
    callEventGrouper: CallEventGrouper;
}
interface IState {
    callState: CallState | CustomCallState;
    silenced: boolean;
    narrow: boolean;
    length: number;
}
export default class CallEvent extends React.PureComponent<IProps, IState> {
    private wrapperElement;
    private resizeObserver;
    constructor(props: IProps);
    componentDidMount(): void;
    componentWillUnmount(): void;
    private onLengthChanged;
    private resizeObserverCallback;
    private onSilencedChanged;
    private onStateChanged;
    private renderCallBackButton;
    private renderSilenceIcon;
    private renderContent;
    render(): JSX.Element;
}
export {};
